registry=https://registry.npmjs.org/
# Wait 7 days before resolving newly published package versions — a supply-chain
# safeguard against freshly-published malicious/compromised packages.
min-release-age=7
