{
  "name": "kb-chat-widget-monorepo",
  "private": true,
  "version": "0.0.0",
  "license": "MIT-0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/kb-chat-core -w packages/kb-chat-react -w packages/kb-chat-widget -w packages/kb-chat-reference-backend",
    "build:all": "npm install && npm run build && npm run typecheck && npm test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p packages/kb-chat-core && tsc --noEmit -p packages/kb-chat-react && tsc --noEmit -p packages/kb-chat-widget && tsc --noEmit -p packages/kb-chat-reference-backend",
    "lint": "eslint packages",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@testing-library/react": "^16.0.1",
    "@types/react": "^18.3.3",
    "eslint": "^9.13.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "jsdom": "^25.0.1",
    "prettier": "^3.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.10.0",
    "vitest": "^3.2.4"
  }
}
